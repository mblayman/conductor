---
app_path: /srv/lcp
venv_path: /srv/virtualenv
ssh_private_key: "{{ vault_ssh_private_key }}"
django_env:
  DJANGO_SETTINGS_MODULE: lcp.settings.staging
  SECRET_KEY: "{{ vault_secret_key }}"
postgres:
  db: lcp
  user: lcp
  password: "{{ vault_postgres_password }}"
