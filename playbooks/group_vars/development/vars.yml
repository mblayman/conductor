---
deployment: development
root_domain: conductor.test
django_settings_module: conductor.settings.staging
google:
  client_id: "{{ vault.google.development.client_id }}"
stripe:
  api_key: "{{ vault.stripe.test.api_key }}"
  publishable_key: "{{ vault.stripe.test.publishable_key }}"
